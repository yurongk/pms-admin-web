<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yurongku.mapper.DeptMapper">

    <sql id="Base_Column_List">
        id
        , dept_name, dept_code, parent_id, ancestors,
        leader, phone, email, status, sort_order, description,
        created_time, updated_time, created_by, updated_by
    </sql>

    <!-- 分页查询 -->
    <select id="selectPageDepts" resultType="com.yurongku.entity.Dept">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_dept
        <where>
            <if test="keyword != null and keyword != ''">
                (dept_name ILIKE CONCAT('%', #{keyword}, '%')
                OR dept_code ILIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY sort_order ASC
    </select>

    <!-- 查询所有 -->
    <select id="selectAllDepts" resultType="com.yurongku.entity.Dept">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_dept
        <where>
            <if test="status != null">
                status = #{status}
            </if>
        </where>
        ORDER BY sort_order ASC
    </select>

</mapper>
