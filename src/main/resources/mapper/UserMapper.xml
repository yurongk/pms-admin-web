<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yurongku.mapper.UserMapper">

    <sql id="Base_Column_List">
        id, username, password, email, phone, real_name, avatar,
        status, last_login_time, last_login_ip, dept_id,
        created_time, updated_time, created_by, updated_by, is_deleted
    </sql>

    <select id="selectPageUsers" resultType="com.yurongku.entity.User">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_user
        <where>
            is_deleted = 0
            <if test="keyword != null and keyword != ''">
                AND (username ILIKE CONCAT('%', #{keyword}, '%')
                  OR real_name ILIKE CONCAT('%', #{keyword}, '%')
                  OR email ILIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <select id="selectAllUsers" resultType="com.yurongku.entity.User">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_user
        <where>
            is_deleted = 0
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY id DESC
    </select>

</mapper>
