<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yurongku.mapper.RoleMapper">

    <sql id="Base_Column_List">
        id, role_name, role_code, description, status, sort_order,
        created_time, updated_time, created_by, updated_by, is_deleted
    </sql>

    <select id="selectPageRoles" resultType="com.yurongku.entity.Role">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_role
        <where>
            is_deleted = 0
            <if test="keyword != null and keyword != ''">
                AND (role_name ILIKE CONCAT('%', #{keyword}, '%')
                  OR role_code ILIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY sort_order ASC, id DESC
    </select>

    <select id="selectAllRoles" resultType="com.yurongku.entity.Role">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_role
        <where>
            is_deleted = 0
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY sort_order ASC, id DESC
    </select>

</mapper>
