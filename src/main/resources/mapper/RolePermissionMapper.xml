<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yurongku.mapper.RolePermissionMapper">

	<sql id="Base_Column_List">
		id, role_id, permission_id, created_time, updated_time, created_by, updated_by, is_deleted
	</sql>

	<delete id="deleteByRoleId">
		DELETE FROM sys_role_permission WHERE role_id = #{roleId}
	</delete>

	<insert id="insertBatch">
		INSERT INTO sys_role_permission (role_id, permission_id, created_time, updated_time, is_deleted)
		VALUES
		<foreach collection="permissionIds" item="pid" separator=",">
			(#{roleId}, #{pid}, NOW(), NOW(), 0)
		</foreach>
	</insert>

	<select id="selectPermissionIdsByRoleId" resultType="long">
		SELECT permission_id FROM sys_role_permission WHERE role_id = #{roleId}
	</select>

</mapper>

